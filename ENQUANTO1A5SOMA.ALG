Algoritmo "ENQUANTO1A5SOMA"

Var
i, soma, num: inteiro

Inicio
i <- 1
Enquanto i <= 5 faca
         escreval("Digite um número do tipo inteiro para a SOMA! ", i, "º vez")
         leia(num)
         soma <- soma + num
         i <- i + 1
FimEnquanto
Escreval("Resultado da soma = ", soma)


Fimalgoritmo